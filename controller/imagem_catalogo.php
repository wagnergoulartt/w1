<?php    require_once ('../database/config.php');    require_once ('../database/config.database.php');    require_once ('../includes/funcoes.php');    $id = get('imagem');    $catalogo = get('catalogo');?><?php $Query = DBRead('fotos_catalogo','*',"WHERE id = '{$id}'"); if (is_array($Query)) { foreach ($Query as $dados) { ?><form method="post" action="?AtualizarImagem=<?php echo $dados['id']; ?>" enctype="multipart/form-data">    <div class="form-group">        <input type="hidden" name="catalogo" value="<?php echo $catalogo; ?>">        <label>Título da Imagem:</label>        <input class="form-control" name="titulo" value="<?php echo $dados['titulo']; ?>" required>    </div>    <center>        <a href="?ExcluirImagem=<?php echo $dados['id']; ?>&catalogo=<?php echo $catalogo; ?>">            <button type="button" class="btn btn-danger">Excluir</button>        </a>        <button class="btn btn-primary">Salvar</button>    </center></form><?php } } ?>